diff --git a/apps/papra-client/src/modules/backups/pages/backups.page.tsx b/apps/papra-client/src/modules/backups/pages/backups.page.tsx
index 9c8480dc..f31d8214 100644
--- a/apps/papra-client/src/modules/backups/pages/backups.page.tsx
+++ b/apps/papra-client/src/modules/backups/pages/backups.page.tsx
@@ -745,7 +745,7 @@ const DestinationCard: Component<{
 
     // Start polling to update status until backup completes
     const interval = setInterval(() => {
-      refetchRuns().then(() => {
+      void refetchRuns().then(() => {
         // Stop polling when there are no more pending or uploading runs
         if (!hasPendingOrUploadingRuns()) {
           clearInterval(interval);
@@ -913,7 +913,7 @@ const DestinationCard: Component<{
               checked={isScheduleEnabled()}
               onChange={(checked) => {
                 setIsScheduleEnabled(checked);
-                saveSchedule({ isEnabled: checked });
+                void saveSchedule({ isEnabled: checked });
               }}
             >
               <SwitchControl>
@@ -932,7 +932,7 @@ const DestinationCard: Component<{
                     class={`text-xs px-2 py-1 rounded border ${days().has(index()) ? 'bg-primary text-primary-foreground border-primary' : 'border-border'}`}
                     onClick={() => {
                       toggleDay(index());
-                      saveSchedule();
+                      void saveSchedule();
                     }}
                   >
                     {label}
@@ -944,7 +944,7 @@ const DestinationCard: Component<{
                 value={String(hour())}
                 onChange={(v) => {
                   setHour(Math.min(23, Math.max(0, Number(v) || 0)));
-                  saveSchedule();
+                  void saveSchedule();
                 }}
               >
                 <TextField type="number" min={0} max={23} />
@@ -955,7 +955,7 @@ const DestinationCard: Component<{
                 value={String(minute()).padStart(2, '0')}
                 onChange={(v) => {
                   setMinute(Math.min(59, Math.max(0, Number(v) || 0)));
-                  saveSchedule();
+                  void saveSchedule();
                 }}
               >
                 <TextField type="number" min={0} max={59} />
diff --git a/apps/papra-server/src/modules/backups/backups.packager.service.ts b/apps/papra-server/src/modules/backups/backups.packager.service.ts
index 3038209f..b6d99124 100644
--- a/apps/papra-server/src/modules/backups/backups.packager.service.ts
+++ b/apps/papra-server/src/modules/backups/backups.packager.service.ts
@@ -103,7 +103,7 @@ function readOctal(buffer: Buffer, offset: number, length: number): number {
   const raw = buffer
     .subarray(offset, offset + length)
     .toString('utf8')
-    .replace(/\0+$/g, '')
+    .replace(/\u0000+$/g, '')
     .trimEnd();
   if (raw.length === 0) {
     return 0;
@@ -116,7 +116,7 @@ function readString(buffer: Buffer, offset: number, length: number): string {
   return buffer
     .subarray(offset, offset + length)
     .toString('utf8')
-    .replace(/\0+$/g, '')
+    .replace(/\u0000+$/g, '')
     .trimEnd();
 }
 
diff --git a/apps/papra-server/src/modules/backups/drivers/google-drive/google-drive.driver.ts b/apps/papra-server/src/modules/backups/drivers/google-drive/google-drive.driver.ts
index 33b009b7..06ddbca3 100644
--- a/apps/papra-server/src/modules/backups/drivers/google-drive/google-drive.driver.ts
+++ b/apps/papra-server/src/modules/backups/drivers/google-drive/google-drive.driver.ts
@@ -47,9 +47,11 @@ export const googleDriveBackupDriverFactory = defineBackupDriver(({ config }) =>
 
     let response: Response;
     try {
+      const headers = new Headers(init.headers);
+      headers.set('Authorization', `Bearer ${accessToken}`);
       response = await fetch(url, {
         ...init,
-        headers: { ...(init.headers ?? {}), Authorization: `Bearer ${accessToken}` },
+        headers,
         signal: timeoutController.signal,
       });
     } catch (error) {
diff --git a/apps/papra-server/src/modules/backups/tasks/backup-retention-cleanup.task.ts b/apps/papra-server/src/modules/backups/tasks/backup-retention-cleanup.task.ts
index 2907a1ab..f6ffadb5 100644
--- a/apps/papra-server/src/modules/backups/tasks/backup-retention-cleanup.task.ts
+++ b/apps/papra-server/src/modules/backups/tasks/backup-retention-cleanup.task.ts
@@ -35,7 +35,7 @@ export async function registerBackupRetentionCleanupTask(deps: GlobalDependencie
     return;
   }
 
-  await taskServices.registerTask({
+  taskServices.registerTask({
     taskName: TASK_NAME,
     handler: async () => {
       const services = createBackupsServices({ config });
diff --git a/apps/papra-server/src/modules/backups/tasks/backup-scheduler-tick.task.ts b/apps/papra-server/src/modules/backups/tasks/backup-scheduler-tick.task.ts
index 06249102..a09243eb 100644
--- a/apps/papra-server/src/modules/backups/tasks/backup-scheduler-tick.task.ts
+++ b/apps/papra-server/src/modules/backups/tasks/backup-scheduler-tick.task.ts
@@ -20,7 +20,7 @@ export async function registerBackupSchedulerTickTask(deps: GlobalDependencies)
     return;
   }
 
-  await taskServices.registerTask({
+  taskServices.registerTask({
     taskName: TASK_NAME,
     handler: async () => {
       const services = createBackupsServices({ config });
